import type { PayrollApprovalQueue } from "@/features/payroll/types";
import { PayrollApprovalCard } from "./payroll-approval-card";
export function PayrollApprovalList({ queue }: { queue: PayrollApprovalQueue }) {
  return <div className="content-stack"><section className="content-stack"><div className="section-heading"><div><h2>Compensation changes</h2><p>Review current and proposed values, effective dates, backdating, and affected periods.</p></div><span className="badge info">{queue.compensation.length}</span></div>{queue.compensation.length ? <div className="payroll-approval-grid">{queue.compensation.map((item) => <PayrollApprovalCard item={item} key={item.id}/>)}</div> : <div className="card empty">No compensation changes are waiting for approval.</div>}</section><section className="content-stack"><div className="section-heading"><div><h2>Payroll schedule assignments</h2><p>Review schedule changes and confirm any mid-period exceptions.</p></div><span className="badge info">{queue.assignments.length}</span></div>{queue.assignments.length ? <div className="payroll-approval-grid">{queue.assignments.map((item) => <PayrollApprovalCard item={item} key={item.id}/>)}</div> : <div className="card empty">No payroll schedule assignments are waiting for approval.</div>}</section></div>;
}
