import type { PayrollPeriodFilters, PayrollScheduleSummary } from "@/features/payroll/types";
import { payrollPeriodStatusValues } from "@/features/payroll/constants";
import { payrollPeriodStatusLabel } from "@/features/payroll/presentation";
export function PayrollPeriodFilterForm({ filters, schedules }: { filters: PayrollPeriodFilters; schedules: PayrollScheduleSummary[] }) {
  return <form className="card payroll-filter-form" method="get"><label>Schedule<select className="field" name="scheduleId" defaultValue={filters.scheduleId ?? ""}><option value="">All schedules</option>{schedules.map((schedule) => <option value={schedule.id} key={schedule.id}>{schedule.name}</option>)}</select></label><label>Status<select className="field" name="status" defaultValue={filters.status ?? ""}><option value="">All statuses</option>{payrollPeriodStatusValues.map((status) => <option value={status} key={status}>{payrollPeriodStatusLabel(status)}</option>)}</select></label><label>Year<input className="field" type="number" min="2000" max="2200" name="year" defaultValue={filters.year ?? ""}/></label><label>From<input className="field" type="date" name="from" defaultValue={filters.from ?? ""}/></label><label>To<input className="field" type="date" name="to" defaultValue={filters.to ?? ""}/></label><div className="form-actions"><button className="btn primary">Apply filters</button><a className="btn" href="/payroll/periods">Reset</a></div></form>;
}
