import { PageHeader } from "@/components/page-header";
export default function SettingsPage(){const settings=['Company profile','Departments','Job titles','Work locations','Employment types','User roles and permissions'];return <><PageHeader title="Settings" description="Configure company structure, access, and HR preferences." /><div className="grid split"><div className="card"><h2 className="card-title">Organization</h2><div className="list">{settings.map(s=><div className="list-item" key={s}><strong>{s}</strong><button className="btn">Manage</button></div>)}</div></div><div className="card"><h2 className="card-title">Frontend status</h2><p className="muted">This build uses mock data only. Authentication, database operations, permissions, uploads, and audit logging are intentionally deferred until backend integration.</p><span className="badge warning">Frontend MVP</span></div></div></>}
